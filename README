Part I
************
This plugin for SourceMod is made to start some configs
after the both team vote. Generally it is made for
L4D1 Rotoblin competition configs as there is no other
way to start a match as from admin menu, or by !execcfg
or by comp_loader plugin. Comp Loader is not approved and
has no source code and it can't start other configs
except of files with 4v4, 2v2 and so on file mask.

To suggest a config a member of one of the team prints: 

!cplay <SuperHardConfig>

Except of !cplay also availiable !cfg and !load

Any player of the other team can confirm the config by
one of the commands:

!confirm

or

!cplay <SuperHardConfig>

Or suggests their own variant:

!cplay <OtherSuperHardConfig>

Server admins can force a config by:

!forceplay <SuperHardConfig>

Then a server starts the config after delay by mask:

<prefix>_<SuperHardConfig>.cfg

Prefix and delay are configurable through CVAR.
***********

Part II
***********

The other usefull function is the ability to a player to
change the team quickly.

For any game:
!switchme - switch player to the opposite team
!spectate - switch to spectators (if L4Downtown plugin is 
            not installed)

For L4D1/2 a player can also use:
!infected - to switch to Infected team
!survivor or !surv - to switch to Survivors team


==========================================================
Часть I
***************
Плагин предназначен для запуска определенных конфигов после
подверждения его обеими командами.

Предполагается, что игрок одной из команд вводит в чат:

!cplay <SuperHardConfig>

Вместо !cplay можно также использовать !cfg или !load

Вторая команда Подверждает предоженный конфиг командой:

!confirm

или

!cplay <SuperHardConfig>

Или предлагает свой вариант аналогичной командой:

!cplay <OtherSuperHardConfig>

Либо предложить свой конфиг, в этом случае первая команда
должна подвердить конфиг аналогичным способом.

Для администраторов должна быть доступна команда:

!forceplay <SuperHardConfig>

После подтверждения сервер включает конфиг после задержки 
по формату:

<prefix>_<SuperHardConfig>.cfg

Префикс (prefix) и задержка задаются параметрами CVAR.
************

Часть II
************
Другой полезной функцией плагина является возможность
игроку сменить команду просто написав в чат:

Для всех игр:
!switchme - перейти в противоположную команду
!spectate - перейти в спектаторы

Для L4D1/2 есть дополнительные:
!infected or !inf - перейти вкоманду Зараженных
!survivor or !surv or !sur - перейти в команду Выживших